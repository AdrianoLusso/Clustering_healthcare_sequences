if (!requireNamespace("TraMineR", quietly = TRUE)) {
  #options(repos = c(CRAN = "http://cran.r-project.org"))
  #install.packages("RColorBrewer")
    install.packages('src/traminer.tar.gz', repos = NULL, type="source")
}
